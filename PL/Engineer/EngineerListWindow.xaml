<Window x:Class="PL.Engineer.EngineerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Engineer"
        mc:Ignorable="d"
        Title="EngineerListWindow" Height="640" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <!--Internal, Upper Grid-->
    <!--Extrnal Grid-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Select:" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Center" Width="400" Height="40" FontSize="20"/>
        <ComboBox Grid.Column="1" SelectedValue="{Binding Level}"
                  ItemsSource="{Binding Source={StaticResource EngineerLevelKey}}" 
                  HorizontalAlignment="Right" Width="400" FontSize="18"/>

        <ListView Margin="5" Grid.Row="1" ItemsSource="{Binding EngineerList}">
            <ListView.View>
                <GridView>
                   
                    <GridViewColumn DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Email}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Level}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Cost}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Task.Id}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Task.Alias}"/>
                        
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Add" HorizontalAlignment="Left" Height="50" Width="100" Margin="127,477,0,97" RenderTransformOrigin="0.508,0.612" Grid.Column="1"/>

    </Grid>

</Window>
